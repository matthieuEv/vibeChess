services:
  vibe-chess-server:
    # image: ghcr.io/matthieuev/vibechess-server:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${HOST_PORT:-8080}:${PORT:-8080}"
    environment:
      PORT: "${PORT:-8080}"
